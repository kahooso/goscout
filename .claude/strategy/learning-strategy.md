# Стратегия обучения

Читается перед каждой выдачей задания (скилл `give-task`).
**Принцип:** план длиной максимум на 4–6 недель. Дальше — пересмотр после v0 goscout.

---

## Главный вектор

**Дорога:** product / infra / deep-tech backend на Go. Двигатель — «создавать новое».
**Кибербезопасность — сильный сквозной угол** и реальная точка входа (стажировка AppSec /
Security Go; Positive — реальный план), один из путей, не единственная узкая цель.
**Сейчас специализацию не выбираю — строю фундамент Go.** Решу позже, по факту
(Positive → больше security; Avito/Ozon/Яндекс → широкий backend).

> Источник истины по карьерному вектору — приватный проект **goagent**
> (`C:\Users\ryche\Desktop\goagent`). Здесь не дублируем; вглубь по карьере — открыть goagent.

**Модель: университет даёт теорию (контекст тем), работа со мной = практика на Go руками.**
- Универ показывает, какие темы сейчас в потоке: структуры данных → сети → ОС → крипта → AI-security.
  Это контекст (что под рукой), не драйвер — направление задаёт вектор (goagent).
- Я беру тему **по своему вектору**, синхронизируя с тем, что универ даёт сейчас, и пишу руками
  **на Go**, без нейронки-за-меня.
- **Ритм: вектор ведёт, универ синхронизирует.** Где вектор и универ совпали по теме — пользуюсь
  совпадением. Не убегать в совсем чужое (Rust/kernel — путь друга, не мой), но и не ждать универ,
  если вектор ведёт вперёд.

**Точки естественного совпадения с семестрами:** [university-plan.md](../../knowledge-base/university-plan.md).

---

## Якорный продукт: goscout (ближняя цель + открытый рост)

> goscout — **не «полигон», а продукт с названной БЛИЖНЕЙ целью**: это снимает паралич
> «не знаю формы». Но дальний горизонт открыт — goscout **растёт вместе со мной**, без воды
> и без привязки к календарю универа. Каждая веха — реально на GitHub.

**Ближняя форма:**
**Сетевой recon / диагностический CLI на Go с security-уклоном.**
Сканирует сеть (DNS / порты / HTTP / TLS), показывает что открыто / как настроено / где небезопасно.
Это прикладная безопасность в виде работающего кода — синергит с профилем ИБ и с CTF-треком.

**Дорожная карта (вехи, без привязки к семестрам):**

| Веха | Что умеет | Статус |
|------|-----------|--------|
| **v0.1** | dns + ports, README, тесты, CI | первый портфолио-проект на GitHub |
| **v1.0** | + HTTP probe, TLS-анализ, security headers | зрелый сетевой инструмент |
| **дальше** | открыто — по мере тем и интереса (service detection? CVE-check? своё) | растёт со мной |

**AI/ML — вне roadmap goscout.** Захочу ML → отдельный осознанный проект, не «потому что
дипломный профиль». **Сейчас:** фундамент Go (A0), затем сетевое ядро v0.1.

### Маппинг тем A0 на фичи goscout (путь к v0.1)

| Тема | Что добавляет в goscout |
|------|-------------------------|
| A0.6 каналы + context | `dns` команда — параллельный resolve N доменов с таймаутом |
| A0.7 интерфейсы | `Probe` интерфейс — общий тип для dns/ports/http проверок |
| A0.8 stdlib (os, bufio, flag) | чтение wordlist для DNS перебора, флаги CLI |
| A0.9 net пакет | `ports` команда — TCP DialTimeout, worker pool |

---

## Текущая позиция

> Обновляется через скилл `session-debrief` после каждого завершённого task.

**Активный блок:** A0 — Go фундамент
**Освоенные темы:** A0.1 структуры, A0.2 слайсы/мэпы, A0.3 указатели, A0.4 ошибки,
A0.5 горутины, A0.6 каналы+select+context (+ фундамент concurrency: race/mutex/atomic,
deadlock vs goroutine leak; + go tooling), A0.7 интерфейсы (неявная реализация, value/pointer
receiver, itable/vtable, полиморфный раннер, typed-nil), A0.8 stdlib CLI (flag.FlagSet,
os.Open+defer, bufio.Scanner, io.Reader для тестов, stdout/stderr+Fprint, коды выхода 0/1/2,
nil vs empty slice)
**Последний task:** task-09 (2026-06-15) — CLI флаги `--timeout`/`--wordlist`, `readTargets(io.Reader)`,
тесты через `strings.NewReader`, всё `-race` зелёное
**Последний мок-собес:** interview-01 (2026-05-17, A0.1–A0.5)
**Счётчик задач с последнего мок-собеса:** 3 *(мок-собес при ≥ 8)*

**Следующая задача:** A0.9 — `net` пакет (TCP/DialTimeout): команда `goscout ports <host> --ports ...`,
параллельный TCP DialTimeout, worker pool через буферизованный канал (см. блок A0 ниже).
Это ПОСЛЕДНЯЯ тема A0 → после неё v0.1 (dns + ports) + blank-slate ретест A0.6 = закрытие блока.

> **A0.9 в процессе — task-10 (сессия 2026-07-04):** фаза 1 (TCP/IP) была пройдена 2026-06-18,
> но за 2.5 недели ВЫВЕТРИЛАСЬ — переактивировали с нуля (IP-адрес vs IP-протокол, IP≠UDP, что
> подтверждает ACK, FIN vs тишина; конспект дополнен в `topics/networks/tcp-ip.md`). Пройдены
> фаза 2 (экскурсия по `net`: DialTimeout/JoinHostPort/Conn.Close + паттерн worker pool) и фаза 3.
> **Написана и зелёным протестирована ПОСЛЕДОВАТЕЛЬНАЯ версия `PortProbe.Run`** (guard + `for` по
> портам + DialTimeout, без горутин); тест `TestPortProbeRunOpen` через `net.Listen("127.0.0.1:0")`.
> Разбор в `topics/go/net.md` + `tasks/task-10.md`. Код: `cmd/goscout/main.go`, `-race` зелёный.
>
> **Стадия 2a ЗАКРЫТА (сессия 2026-07-06):** `PortProbe.Run` переписан с последовательного `for`
> на **worker pool** сам, руками (N=100 воркеров, `jobs`/`results` буфер `len(Ports)`, `close(jobs)`,
> сборщик на ровно N, результат слать на КАЖДЫЙ порт). Новый тест `TestPortProbeRunClosed` ловит
> рассинхрон/deadlock. Всё `-race` зелёное. Каналы (A0.6) снова ВЫВЕТРИЛИСЬ — переактивировали с
> нуля (`<-` send/receive, буфер vs небуфер, `close`) → подтверждает нужду в blank-slate ретесте A0.6.
>
> **ПРОДОЛЖИТЬ С (ступень 2b, короткая):** сортировка `open`. НАЧАТЬ с контрольного вопроса, что
> завис незаданным: «если сортировать `open []string` КАК СТРОКИ — какой порядок у `"80"` и `"443"`?»
> Подвести к лексикографике (`"443" < "80"` посимвольно → баг сортировки портов); решение —
> сортировать ЧИСЛЕННО (держать порты числами + `slices.Sort`/`sort.Slice` до конвертации в строки).
> + тест на порядок вывода. Потом **ступень 3 — CLI:** `parsePorts` (CSV портов, валидация 1..65535)
> + ветка `case "ports"` с флагами `--ports`/`--timeout` (сейчас старая заглушка `runProbe(PortProbe{},...)`).
> После 2b+3 task-10 закрыт → счётчик мок-собеса +1 (станет 4) → blank-slate ретест A0.6 → закрытие A0.
>
> **ПОДАЧА (важно, зафиксировано 2026-07-06):** вести СТУПЕНЯМИ по одной; НЕ давать оторванные
> учебные примеры в вакууме (песочница `chanplay` раздражила — работать в ЕГО `main.go`); БЕЗ
> аналогий C/C++ и слова «труба» (раздражают — memory `feedback_teaching_style`). Сети = его
> семестр 4 + личный интерес — прямо по вектору.

> Уверенность A0.8 (со слов 2026-06-15): «не так уж сложно, основательно затронули теорию».
> Тема прошла легче A0.7 — подтверждает «теория ПЕРЕД заданием». ПРОБЕЛ: `os`/потоки —
> базовое понимание есть, не автоматизм. Держать для ретеста при закрытии A0.

> Уверенность по A0.6 каналам: «понял с подсказками, с нуля не уверен» — держать в уме
> для blank-slate ретеста при закрытии A0. A0.7 интерфейсы: «зачем интерфейс» долго не
> сходилось, но донеслось через аналогию file_operations — закрыто.

### Точечные пробелы — отслеживать
- (закрыто в task-07) Closure: захват **по ссылке всегда**; в 1.22 поменяли число
  переменных цикла, не способ захвата.
- `errors.Is(ErrFoo, ErrFoo)` → true (для самой ошибки, не только обёрнутой) — был пробел
  на interview-01, отдельно не ретестили в task-07. Проверить на следующем мок-собесе.

Полный отчёт: [interview-01.md](../../knowledge-base/interviews/interview-01.md).

---

## Финал блока A0

Когда A0.6–A0.9 закрыты — `goscout` **v0.1** работает (dns + ports). Это первая запись
в GitHub-портфолио и первая веха дорожной карты (см. «Якорный продукт» выше).
Дальше — сетевое ядро (синергит с сем 4 «Сети»). **Ближняя форма известна (сетевой recon CLI),
дальний горизонт открыт — детально на 2+ года не планируем.**

Каждая задача A0.6–A0.9 **добавляет фичу в goscout**, не создаёт отдельный `cmd/<thing>/`.

**Гейт закрытия блока A0 (blank-slate ретест).**
Блок A0 не считается закрытым, пока не пройден ретест самой сложной темы блока —
**каналы + context (A0.6)** — с нуля, **без vault и без подсказок Claude**.
Задача формулируется заново (например: «параллельный resolve N доменов с таймаутом
через канал + `context.WithTimeout`, собрать результаты через `select`»),
пользователь пишет с чистого листа. Если не пишется без подсказок — точечный возврат
к теме, ретест повторяется. Только после прохождения блок закрыт.
Проверяет главное: можешь ли писать без подсказок (`feedback_think_independently.md`).
Детали процедуры — в скилле `mock-interview` (раздел «Blank-slate ретест»).

---

## Принципы

1. **Проект раньше теории** — пишем `goscout`, темы изучаем как фичи для него.
2. **Теория раньше кода** — три фазы (концепция → пакет → контрольные вопросы) обязательны до задания.
3. **Одна задача = одна фича scout + 1–2 темы.** Не смешивать больше.
4. **Каждая задача → тесты** — минимум 3 кейса: happy + граничный + злой. Для concurrency — `-race`.
5. **Каждая задача → vault** — иначе знание не зафиксировано.
6. **Каждые 8 задач — мок-собес** по пройденным темам.
7. **Не уверен в теме — закрепление, не следующая.**
8. **Скучно — говорить сразу.** Не тянуть нелюбимую тему ради галочки.
9. **Длинные комментарии в коде → vault.** В `cmd/*.go` остаётся только что объясняет именно этот фрагмент.
10. **Algo-практика — параллельная ветка** (см. ниже). Не заменяет основной план A0/A/B, но
    вкрапляется между задачами для закрепления базы и тренировки собеседовательного формата.
11. **English-словарь** — каждая новая запись в `topics/` имеет раздел
    `## Ключевые термины (English)`. Названия функций/пакетов/протоколов — всегда на английском.
    Это словарь для собесов (PT, Яндекс, Bi.Zone) и чтения RFC/доков. Детали — в принципах ниже.
12. **CTF — параллельный трек** (picoCTF, фокус Web + General). Практика AppSec руками,
    синергия с OWASP Top 10. Инициирует только пользователь. Детали — в скилле `ctf` и разделе ниже.

---

## Algo-практика — параллельная ветка

**Зачем:** закрепление базы знаний (slices/maps/pointers/strings) через мини-задачи
в формате собеседования. Это **не** новая большая тема — это «пятиминутки на удержание
формы». Параллельно основной линии A0/A/B.

**Когда вкраплять:**
- Когда пользователь говорит «дай алго-задачку», «давай LeetCode», «хочу размяться»
- Когда после задачи A0 осталось 30–60 минут сессии и брать новую тему рано
- Когда после мок-собеса видны пробелы по базовой теме — алго-задача на этом паттерне
- Не чаще ~1 algo-задачи на 2–3 задачи блока A0 — не отвлекать от основной линии

**Где хранить:**
- Код: `cmd/tasks/<имя>/<имя>.go` + `<имя>_test.go` (пакет = имя задачи, не `main`)
- Запись: `knowledge-base/tasks/algo-NN-<имя>.md` — отдельная нумерация
- Паттерн (если новый): `knowledge-base/topics/algo/<паттерн>.md`

**Тематический бэклог** (когда давать какую):

| # | Задача | Паттерн | Когда уместно |
|---|--------|---------|----------------|
| algo-01 | Two Sum | map-as-index | ✅ выполнено (2026-05-17) |
| algo-02 | Contains Duplicate | map-as-set | базовый разгон после Two Sum |
| algo-03 | Valid Anagram | map подсчёт частот | после strings/maps |
| algo-04 | Reverse Linked List | указатели + рекурсия/итерация | после A0.3 закрепления указателей |
| algo-05 | Move Zeroes | two-pointers | новый паттерн |
| algo-06 | Longest Substring Without Repeating | sliding-window | средний уровень, после A0.6 |
| algo-07 | Group Anagrams | map[string][]string | средний уровень |
| algo-08 | Top K Frequent Elements | map + sort или heap | связь с sort.Slice |

Не давать все подряд. Выбирать в зависимости от того где пробелы или интерес.

**Формат сессии под algo-задачу (~30–60 мин):**
1. Постановка (1–2 мин) — задача коротко, сигнатура, пример
2. Сократовский разговор про подход (5–10 мин) — наивный O(?) → оптимальный
3. Пользователь пишет код + тесты (~20 мин)
4. Code review через скилл `code-review`
5. Запись в `tasks/algo-NN.md` через `vault-write`

**Чего НЕ делать:**
- Не давать алго вместо темы A0 если пользователь явно не попросил
- Не превращать в марафон по LeetCode — это **закрепление**, не основная цель
- Не делать алго с темой которую пользователь ещё не проходил (например graph BFS до интерфейсов)

---

## CTF-практика — параллельная ветка

**Зачем:** security / AppSec — один из твоих путей и профиль универа. CTF (picoCTF) — это практика
безопасности **руками**, под атакующим углом. Не для накопления флагов, а чтобы
OWASP Top 10 был из опыта, а не из статьи, и чтобы набить рабочий словарь утилит.

**Фокус — НЕ как у fn2s.** Друг (builder СЗИ) приоритезирует reverse/pwn/forensics.
У тебя цель — веб и приложения, поэтому:
- **Главное:** Web Exploitation (SQLi, XSS, SSRF, IDOR, JWT) + General Skills.
- **Можно:** лёгкая крипта (base64/XOR/Caesar).
- **Пропускать:** reverse, pwn, тяжёлую крипту, stego — не вектор (и неинтересно по CLAUDE.md).

**Темп:** 1–2 челленджа в неделю, 60–90 мин. Цель-ориентир: 30+ к концу 2026.
Инициирует **только пользователь** — счётчиков нет, проактивно не напоминать.

**Хранение:** `knowledge-base/ctf/picoctf/` (сессии + writeup'ы по категориям).
Каждый web-writeup привязан к OWASP-категории. Детали процедуры — скилл `ctf`.

**Синергия с goscout:** сетевые/HTTP-челленджи прямо пересекаются с будущей командой
`goscout http` (security headers, TLS). Сначала понять руками что заголовки защищают.

---

## English — рабочий технический словарь

**Зачем:** собесы в PT / Яндекс / Bi.Zone / Kaspersky и чтение RFC, доков, CVE — на английском.
Цель — выстроить технический словарь через регулярное использование, не отдельным курсом.

**Правила:**
- Каждая новая запись в `topics/` имеет раздел `## Ключевые термины (English)` — глоссарий темы.
- Названия функций, пакетов, флагов, типов — **только английский**, не транслитерировать
  (`context.WithTimeout`, `DialTimeout`, `race condition`, не «дайлтаймаут»).
- Ключевые концепции при первом упоминании в объяснении — англ. термин + русский в скобках:
  «состояние гонки (race condition)», «таймаут контекста (context timeout)».
- Старые `topics/` не переписывать массово — глоссарий добирается естественно при редактировании.

Это правило исполняется через скилл `vault-write` (шаблон `topics/` содержит раздел глоссария).

---

## Блок A0 — оставшиеся темы

Формат каждой задачи в A0:
1. Фаза 1: разбор концепции (15–25 мин) — обязательно с аналогиями C/C++
2. Фаза 2: экскурсия по пакету (10–15 мин) — открыть `pkg.go.dev`, не выдумывать функции
3. Фаза 3: контрольные вопросы (5–10 мин) — по одному, ждать ответа
4. Задание — фича в `cmd/goscout/`
5. Тесты — 3+ кейса
6. `vault-write` — обновить `tasks/task-NN.md` и `topics/go/<тема>.md`

### A0.6 — каналы + context (объединённая тема)

**Почему вместе:** каналы без context = неполное знание; в реальном Go горутины,
каналы и context всегда вместе.

**Фича в scout:** `goscout dns <domain1,domain2,...> --timeout 5s`
- запускает N горутин-резолверов (по одной на домен)
- каждая пишет результат в канал
- main собирает результаты через `select { case <-results: ... case <-ctx.Done(): ... }`
- `context.WithTimeout` останавливает зависшие горутины

**Пакеты:** `sync` (уже знаком), `context`, встроенные каналы.
**Что нового по сравнению с task-06:** `select`, `close(chan)`, `range ch`, `context.WithTimeout`.

### A0.7 — интерфейсы

**Фича в scout:** ввести интерфейс `Probe`:
```go
type Probe interface {
    Run(ctx context.Context, target string) (Result, error)
    Name() string
}
```
Реализации: `DNSProbe`, заглушка `PortProbe`. Регистрация в main через слайс `[]Probe`.

**Что нового:** неявная реализация интерфейсов, `io.Reader`/`io.Writer` как главные примеры stdlib.

### A0.8 — stdlib (os, bufio, flag)

**Фича в scout:** флаги `--wordlist file.txt`, `--timeout`, чтение wordlist построчно через `bufio.Scanner`.

**Пакеты:** `flag` (или `os.Args` для начала), `os`, `bufio`, `errors`.

### A0.9 — net пакет (TCP/DialTimeout)

**Фича в scout:** команда `goscout ports <host> --ports 80,443,22,8080`.
- параллельный TCP DialTimeout
- worker pool через буферизованный канал

**Пакеты:** `net.DialTimeout`, `net.JoinHostPort`.

**Итог:** v0.1 готов. Дальше — пересмотр плана.

---

## Что после A0

> Не планируем сейчас. Когда `goscout` v0.1 заработает — садимся, смотрим что выучили,
> где пробелы, и что хочется добавить (HTTP probe? TLS analysis? Service detection?
> CVE check?). Стратегия пишется задним числом по реальному состоянию, не вперёд по фантазии.

Карьерные ориентиры (контекст, не план; детально по карьере — goagent):
- **Сейчас**: фундамент Go (A0) + algo; `goscout` v0.1 (dns+ports) на GitHub с тестами и CI.
- **Сети (сем 4)**: сетевое ядро goscout синергит с предметом — двойное усиление.
- **Дальше**: goscout v1 (HTTP/TLS), CTF web-челленджи; стажировка (~2027) — AppSec/Security Go
  **или** широкий backend, решается по факту.
- Глубже 2 курса детально не планируем — см. goagent.

---

## Логика выбора следующей задачи

```
1. Открыть «Текущая позиция»
2. Счётчик ≥ 8 → предложить мок-собес
3. Иначе → следующая тема в текущем блоке
4. Спросить уверенность в предыдущей теме
   неуверен → задача-закрепление
5. Три фазы → задание-фича в goscout → тесты → vault
```

---

## Мок-собесы

Каждые 8 задач или по запросу. Формат:
- 5–7 вопросов смешанных типов: «что делает X», «почему так», «что если…», «найди баг»
- По одному, ждать ответа, разбирать каждый
- Фиксация в `knowledge-base/interviews/interview-NN.md`
- Слабые темы → вернуться к `topics/`
