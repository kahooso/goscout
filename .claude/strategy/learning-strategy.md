# Стратегия обучения — fn2s

Этот файл читается перед выдачей каждого задания (скилл `give-task`).
Здесь — полный учебный план, текущая позиция и логика движения вперёд.

---

## Текущая позиция

> Обновлять после каждого завершённого task (скилл `session-debrief`).

**Активный блок:** A — Go для сетевой безопасности
**Выполненные темы блока A:** нет
**Последний выполненный task:** нет

**Следующая задача — A1: TCP/UDP клиент-сервер на Go**
- Написать TCP echo-сервер + клиент, потом UDP версию
- Пакеты: `net` (Listen, Dial, Accept, ReadFrom/WriteTo)
- Код: `go/tools/`
- Vault: `knowledge-base/tasks/task-01.md`, тема: `topics/go/net-package.md`

**Счётчик задач с последнего мок-собеса:** 0
*(мок-собес каждые 8 задач — при достижении предложить `mock-interview`)*

---

## Главный вектор

**Go → AppSec → Security Tools**

Навыки учатся не сами по себе — они учатся под конкретный проект.
Каждый этап заканчивается живым якорным проектом.

| Этап | Якорный проект | Когда |
|------|----------------|-------|
| 1. Фундамент | HTTP Security Scanner (Go) | сейчас — конец 2026 |
| 2. Сети | Network Scanner + совместный проект с другом | 2027 |
| 3. Специализация | CTF (web/network) + первый оффер | 2027–2028 |

**Цель:** первый оффер к лету–осени 2028 (конец 3-го курса).

---

## Принципы

1. **Проект раньше теории** — не "учу HTTP", а "пишу security scanner". Инструмент — мотивация.
2. **Go везде** — все задачи пишутся на Go, кроме явных исключений.
3. **Код раньше чтения** — сначала написать и сломать, потом идти в документацию.
4. **Одна тема — один task** — не смешивать горутины и SQL injection в одном задании.
5. **Каждый task завершается vault-записью** — иначе знание не зафиксировано.
6. **Раз в 8 задач — мок-собес** — по пройденным темам, формат вопрос-ответ без кода.
7. **Скучное — говорить сразу** — если тема неинтересна, обсудить, не тянуть.

---

## Этап 1 — Фундамент (сейчас — конец 2026, ~8–10 месяцев)

**Цель этапа:** уметь писать security инструменты на Go, знать OWASP Top 10, понимать сетевой стек до уровня пакетов.

**Якорный проект: HTTP Security Scanner на Go**
Проверяет URL на: наличие security headers, конфигурацию TLS, базовые признаки уязвимостей.
Не клон Burp Suite — свой инструмент с пониманием каждой строки.
Блоки A и B работают на него.

**Связь с универом:**
- Алгоритмизация и программирование → Go алгоритмы напрямую используются
- Сети и ОС → блок C пересекается с теорией курса
- Основы ИБ → блок B покрывает уязвимости которые там разбираются

---

### Блок A — Go для сетевой безопасности [ТЕКУЩИЙ]

Освоенные темы (не давать снова):
- нет

**Следующие темы по порядку:**

#### A1 — Go net package: TCP/UDP клиент-сервер
Цель: понять как Go работает с сетью на уровне сокетов.
Задание: TCP echo-сервер + клиент; потом UDP версия.
Пакеты: `net.Listen`, `net.Dial`, `net.PacketConn`
Код: `go/tools/`

#### A2 — Параллельность для сетевых инструментов
Цель: научиться запускать параллельные сетевые подключения — основа любого сканера.
Задание: TCP-сервер обрабатывает несколько соединений через горутины; worker pool через каналы.
Паттерны: goroutine per connection, WaitGroup, done channel, context с таймаутом
Код: `go/tools/`

#### A3 — Port scanner на Go
Цель: первый настоящий security tool — объединяет A1 и A2.
Задание: параллельный port scanner с таймаутом, worker pool, вывод открытых портов.
Пакеты: `net.DialTimeout`, goroutines + channels
Код: `go/scanner/`

#### A4 — HTTP клиент в Go
Цель: научиться делать HTTP-запросы с полным контролем — заголовки, TLS, редиректы.
Задание: кастомный HTTP клиент: таймаут, кастомные заголовки, отключение редиректов, анализ TLS сертификата.
Пакеты: `net/http`, `crypto/tls`, `crypto/x509`
Код: `go/tools/`

#### A5 — Security Headers Scanner
Цель: понять HTTP security headers и уметь программно их проверять.
Задание: инструмент проверяет URL — наличие и корректность HSTS, CSP, X-Frame-Options, X-Content-Type-Options.
Пакеты: `net/http`
Код: `go/scanner/`

#### A6 — DNS enumeration
Цель: понять DNS на практике, написать реальный reconnaissance инструмент.
Задание: subdomain enumerator — wordlist, параллельные DNS-запросы через горутины, вывод живых поддоменов.
Пакеты: `net` (LookupHost, LookupCNAME)
Код: `go/scanner/`

**Финал блока A — якорный мини-проект:**
Объединить A3 + A5: port scanner с автоматическим HTTP security headers check на открытых веб-портах.

---

### Блок B — AppSec основы [после A3–A4]

#### B1 — OWASP Top 10
Цель: знать топ уязвимостей веба, уметь объяснить каждую своими словами.
Формат: для каждой из 10 — что это, пример уязвимого кода, как защититься в Go.
Практика: WebGoat (Docker) — пройти задания по теме.
Vault: `topics/security/owasp-top10.md`

#### B2 — SQL Injection
Цель: понять SQLi глубоко — как уязвимость и как детектировать.
Задание: Go SQLi detector — анализирует HTTP-параметры URL на наличие паттернов инъекции.
Пакеты: `regexp`, `net/http`
Код: `go/scanner/`

#### B3 — XSS, CSRF, SSRF
Цель: разобрать три самых частых веб-уязвимости.
Формат: теория + практика на WebGoat + добавить проверки в свой scanner.
Vault: `topics/security/xss.md`, `topics/security/csrf.md`, `topics/security/ssrf.md`

#### B4 — Auth уязвимости: JWT и сессии
Цель: понять как ломают авторизацию.
Задание: Go-инструмент для анализа JWT токенов — decode без библиотеки, проверка alg=none, expired.
Пакеты: `encoding/base64`, `encoding/json`
Код: `go/tools/`

#### B5 — Broken Access Control и IDOR
Цель: понять горизонтальное и вертикальное повышение привилегий.
Формат: теория + WebGoat + добавить проверку в scanner (предсказуемые ID в URL).

#### B6 — Финал: HTTP Security Scanner v1
Объединить A5, B1–B5: один инструмент который по URL выдаёт security-отчёт.
Проверяет: headers, TLS версию, признаки SQLi в параметрах, JWT анализ если есть токен.
Место: `go/scanner/http-scanner/`

---

### Блок C — Сети углублённо [после B или параллельно]

#### C1 — TCP/IP refresher: от пакета до handshake
Цель: освежить сетевой стек, понять что именно происходит при TCP соединении.
Формат: разбор через Wireshark на реальном трафике + vault запись.
Vault: `topics/networks/tcp-ip.md`

#### C2 — gopacket: захват и анализ пакетов
Цель: научиться работать с сырыми пакетами из Go.
Задание: захват трафика на интерфейсе, вывод src/dst/port для TCP пакетов.
Библиотека: `github.com/google/gopacket`
Код: `networking/`

#### C3 — DNS вручную
Цель: понять протокол DNS изнутри — пригодится в сканере.
Задание: собрать DNS UDP запрос вручную (без net.LookupHost), отправить, распарсить ответ.
Пакеты: `net` (UDPConn), `encoding/binary`
Код: `go/tools/`

#### C4 — TLS internals
Цель: понять TLS 1.3 handshake без написания своей реализации.
Формат: Wireshark на HTTPS-сессии + разбор certificate chain + vault запись.
Vault: `topics/networks/tls.md`, `topics/security/tls-attacks.md`

#### C5 — Финал блока C: Network Scanner на Go
ARP ping + TCP port scan + banner grabbing для определения сервиса.
Это совместный проект с другом: его C-агент (низкоуровневый ARP/raw sockets) + твой Go-слой (API, агрегация, отчёт).
Код: `networking/scanner/`

---

### Блок D — DevSecOps [параллельно с B или C]

#### D1 — GitHub Actions: CI/CD для Go
Задание: pipeline — go build, go test, go vet на каждый push.
Место: `.github/workflows/`

#### D2 — gosec: SAST для Go
Задание: запустить gosec на своём scanner, разобрать и исправить все находки.
Цель: понять какие ошибки ловит статический анализ.

#### D3 — semgrep: кастомные правила
Задание: написать semgrep-правило для поиска потенциальных SQLi паттернов в Go-коде.
Цель: понять как работает SAST изнутри.

#### D4 — Docker security
Задание: Dockerfile для security scanner — non-root, minimal image, сканирование образа через trivy.

#### D5 — SBOM
Задание: сгенерировать SBOM для своего Go проекта через syft или go-cyclonedx.

---

## Этап 2 — Углублённый AppSec (2027, ~10 месяцев)

**Якорный проект: полноценный Network Security Monitor (совместный с другом)**
Go-управляющий сервер + C-агент от друга (мониторинг /proc, сетевых соединений).
API сбора событий, хранение, алертинг, простой дашборд.

**Что нужно освоить до начала:**
- Блок C завершён (gopacket, DNS, TLS)
- Знание OWASP Top 10 на уровне объяснения без подсказок
- Базовый DevSecOps pipeline (D1–D2)

---

### Блок E — Advanced AppSec

#### E1 — HackTheBox / TryHackMe: web challenges
Цель: проверить знания AppSec в боевом контексте.
Формат: 5+ web-задач на HackTheBox (Starting Point) или TryHackMe.
Фиксировать: writeup для каждого решённого challenge в `knowledge-base/interviews/`.

#### E2 — Authentication глубоко: OAuth 2.0 / OpenID Connect
Задание: написать Go OAuth 2.0 клиент с нуля (без библиотек для flow).
Цель: понять уязвимости OAuth (open redirect, state bypass, token leakage).

#### E3 — API Security: REST и GraphQL
Цель: уязвимости API — BOLA, mass assignment, rate limiting bypass.
Задание: написать Go API security checker — проверяет типичные мисконфигурации REST API.

---

### Блок F — Совместный проект с другом

#### F1 — Go API для C-агента друга
Задание: HTTP сервер на Go принимает события от C-агента друга (JSON).
Пакеты: `net/http`, `encoding/json`

#### F2 — Хранение и обработка событий
SQLite или файловое хранилище, агрегация событий по типам.

#### F3 — Финал: Network Security Monitor
Полный цикл: агент (C/Rust, друг) → API (Go, ты) → хранилище → алерты.
Место: `networking/monitor/`

---

## Этап 3 — Специализация (2027–2028)

**Цель:** первый оффер. Два пути — определится по итогам этапа 2.

**Путь A — AppSec Engineer:**
- Bug bounty (HackerOne, bugcrowd) на веб-программах
- Участие в пентест-проектах на стажировке
- CVE research или написание PoC для публичных уязвимостей

**Путь B — Security Go Developer:**
- Вклад в опен-сорс security-инструменты (nuclei, subfinder, httpx)
- Написать собственный инструмент и опубликовать
- Участие в CTF (web/network категории)

---

## Ресурсы

| Ресурс | Этап | Формат |
|--------|------|--------|
| OWASP Testing Guide | 1–2 | онлайн, бесплатно |
| Web Application Hacker's Handbook | 1–2 | книга |
| Beej's Guide to Network Programming | 1 | онлайн, бесплатно |
| The Go Programming Language (Donovan & Kernighan) | 1 | книга |
| HackTheBox / TryHackMe | 2–3 | платформа |
| HackerOne Hacktivity | 2–3 | онлайн, реальные отчёты |

---

## Чекпойнты

**Конец 2026 (этап 1):**
- [ ] HTTP Security Scanner: проверяет headers, TLS, базовые признаки уязвимостей
- [ ] Port scanner: параллельный, с таймаутом, с banner grabbing
- [ ] Можешь объяснить OWASP Top 10 на словах без шпаргалки
- [ ] GitHub: 30+ осмысленных коммитов с читаемой историей

**Середина 2027 (этап 2):**
- [ ] Network Security Monitor работает совместно с C-агентом друга
- [ ] Решил 5+ web-задач на HackTheBox
- [ ] CI/CD pipeline с gosec + semgrep для своего проекта

**К офферу (лето–осень 2028):**
- [ ] 2–3 живых проекта на GitHub с README
- [ ] Можешь пройти технический скрининг по Go и AppSec
- [ ] Участвовал в bug bounty или внёс вклад в security опен-сорс

---

## CTF

После блока B: **PicoCTF** (web категория, beginner, бесплатно).
После блока C: **HackTheBox Starting Point** (web + network).
Цель — не стать CTF-плеером, а проверить AppSec-знания в боевом контексте.
1–2 CTF в семестр, параллельно с основным планом.

---

## Логика выбора следующего задания

```
1. Где сейчас? → читай 00-roadmap.md
2. Какой последний выполненный task? → найди его тему в этом файле
3. Следующая тема по порядку в текущем блоке
4. Если блок завершён → переходи к следующему блоку
5. Масштаб задания → по времени из скилла give-task
```

---

## Мок-собесы

Каждые 8 задач (или по запросу) проводить мок-собес:
- 5–7 вопросов по пройденным темам
- Формат: вопрос → пауза → ответ → разбор
- Фиксировать в `knowledge-base/interviews/`
- Слабые места → вернуться к соответствующей теме в topics/

---

## Обновление этого файла

Обновлять когда:
- Завершён целый блок → отмечать как выполненный
- Появилась новая тема которую стоит добавить
- Изменился приоритет или вектор

---

## Карьерная стратегия (обновлено 2026-04-25)

### Основная цель: AppSec инженер / Security Go Developer

**AppSec инженер:** находит и закрывает уязвимости в коде и инфраструктуре. Делает code review с security фокусом, настраивает SAST/DAST, обучает разработчиков.
**Security Go Developer:** пишет инструменты безопасности — сканеры, агенты, детекторы. Близко к разработке, но с security контекстом.

Оба пути ценятся. Первые 1–2 года обучения общие, специализация проявится сама.

Что НЕ нравится: ручной пентест, red team, reverse engineering — эти направления не включать в план без явного запроса.

### Точка входа: Junior AppSec / Security Go developer intern (2–3 курс, ~2026–2027)

**Что это:** встраивание безопасности в разработку — code review, SAST, тестирование безопасности API.
**Компании:** Positive Technologies, Bi.Zone, Яндекс (безопасность продуктов), Kaspersky (разработка), Сбер (продуктовая безопасность).
**Что ищут:** умеет читать/писать Go, знает OWASP, понимает HTTP/TLS, может найти SQLi в коде.

### Важно

DevSecOps — стартовая площадка, не конечный пункт. Если окажется интересно — углублять. Если нет — оставить как базовый навык.
fn2s roadmap продолжается параллельно с любой стажировкой.
