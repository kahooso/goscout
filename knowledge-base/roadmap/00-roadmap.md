---
обновлено: 2026-08-20
---

# Roadmap — диспетчер

**Где я сейчас и что дальше.** Самый часто читаемый файл — держится коротким.
Обновляется скиллом `session-debrief` после каждой сессии. **Primary source** для всех
скиллов: `give-task`, `mock-interview`, `ctf`.

| Нужно | Файл |
|---|---|
| Что сделано, хронология, разборы сессий | [journal.md](journal.md) |
| Закрытые блоки, что усвоено | [archive.md](archive.md) |
| План будущих блоков и принципы | [../strategy/learning-strategy.md](../strategy/learning-strategy.md) |
| Карта конспектов | [../topics/_index.md](../topics/_index.md) |
| Кто пользователь, реестр скиллов | [../../.claude/CLAUDE.md](../../.claude/CLAUDE.md) |

Код и разборы заданий: `sections/<домен>/<имя>/` · конспекты: `knowledge-base/topics/`
Уровень по Go — memory `user_go_level` (источник истины, здесь не дублируется).

---

## Текущая позиция

**Активный блок:** нет. Блок A0 закрыт 2026-07-28 ([archive.md](archive.md)).
Следующий блок не выбран — это решение уровня вектора, здесь оно не назначается.
Кандидат, зафиксированный ранее: «база Positive» (Linux → сети → администрирование →
веб-безопасность), проектируется скиллом `plan-block`.

**Последний task:** task-12 (2026-07-30) — аудит `ports` перед тегом v0.1: отмена контекста
в `PortProbe.Run`, инъекция `Dialer` с фолбэком, тест на отмену, раскладка `main.go` по файлам.
Разбор — [../tasks/task-12.md](../tasks/task-12.md).

**Последний мок-собес:** retest-01 (2026-07-28, blank-slate ретест A0.6 — пройден холодным
заходом). **Счётчик задач с последнего мок-собеса: 1** *(мок-собес при ≥ 8)*.

---

## Следующий шаг

**1. outpost → тег v0.1.** Сборка сейчас красная: незакоммиченный рефакторинг `runProbe`
под `io.Writer` не компилируется (`undefined: ErrOut`, `missing return` в `main.go`).
Дальше по `BACKLOG.md`: тест вывода в `bytes.Buffer`, честный пример в README
(`outpost ports --ports 135,445,9999 127.0.0.1` → `127.0.0.1 -> 135, 445`), файл `LICENSE` (MIT).

**2. Перестройка goscout** — очередь в [../../.claude/upgrade-plan.md](../../.claude/upgrade-plan.md).

**3. Стажировка PT Start 2026** — трек «с обучением», дедлайн заявки 20.08.2026.
Условия и треклист — в локальном рабочем документе (в репозиторий не публикуется).

---

## Слабые места — держать в поле зрения

**Сквозная тема №1: ТЕСТИРОВАНИЕ. Подтверждена как главная слабость — task-12 провален по ней.**
Не часть A0, но проседает сильнее всего. Формы, встреченные подряд: тавтологические проверки
(retest-01), **ранний выход как источник ложно-зелёного** (task-12: тест на отмену не доходил
до проверяемого кода), заглушка с нарушенным контрактом зависимости, тест на отмену с мёртвым
контекстом на входе (не различает версии кода). Мутационную проверку знает, но **сам ни разу
не прогнал** — мутанта запускал Claude, и тот выжил. Тест `TestPortProbeRunCanceled` в итоге
писал Claude по прямой просьбе (пользователь устал, сказал «ничего не понятно»).
Разобрано в [../topics/go/testing.md](../topics/go/testing.md) (разделы «Ложно-зелёный из-за раннего выхода», «Заглушка
обязана соблюдать контракт», «Отмена: контекст должен умирать ПО ХОДУ»).
**Как брать дальше:** маленькими вставками к задачам, но с жёстким ритуалом — перед каждым `if`
в тесте отвечать вслух на вопрос «какая поломка кода покрасит эту проверку», и прогонять
мутацию **своими руками**, а не принимать на слово. Отдельным блоком не выделять.


### Точечные пробелы

- `errors.Is(ErrFoo, ErrFoo)` → true (для самой ошибки, не только обёрнутой) — был пробел
  на interview-01, отдельно не ретестили в task-07. Проверить на следующем мок-собесе.

Полный отчёт: [interview-01.md](../interviews/interview-01.md).

