---
обновлено: 2026-08-20
---

# Journal — журнал выполненного

Что сделано: задания, мок-собесы, CTF, инфра-сессии. Сюда пишут скиллы при закрытии работы.
Позиция и следующий шаг — [00-roadmap.md](00-roadmap.md). Пройденные темы — [archive.md](archive.md).

## Журнал задач блока A0

| # | Блок | Название | Статус | Дата |
|---|------|----------|--------|------|
| 01 | A0.1 | Структуры и методы | ✅ выполнено | 2026-04-26 |
| 02 | A0.2 | Слайсы и мэпы — счётчик слов | ✅ выполнено | 2026-04-28 |
| 03 | A0.2-bis | Закрепление: парсер CSV-логов | ✅ выполнено | 2026-05-07 |
| 04 | A0.3 | Указатели — изменяемый стек | ✅ выполнено | 2026-05-09 |
| 05 | A0.4 | Обработка ошибок — парсер конфига | ✅ выполнено | 2026-05-10 |
| 06 | A0.5 | Горутины — параллельный обработчик задач | ✅ выполнено | 2026-05-16 |
| 07 | A0.6 | Каналы + select + context — параллельный DNS resolve (`goscout dns`) | ✅ выполнено | 2026-06-06 |
| 08 | A0.7 | Интерфейсы — `Probe` контракт, полиморфный раннер (dns + ports-заглушка) | ✅ выполнено | 2026-06-10 |
| 09 | A0.8 | stdlib CLI — флаги `--timeout`/`--wordlist`, чтение wordlist (`os`/`bufio`/`io.Reader`) | ✅ выполнено | 2026-06-15 |
| 10 | A0.9 | net + worker pool — `goscout ports` (TCP-скан, `parsePorts`, числовая сортировка, унификация вывода) | ✅ выполнено | 2026-07-10 |

**Блок A0 ЗАКРЫТ 2026-07-28.** Оба гейта взяты: устный мок-собес A0.6–A0.9 (interview-02,
2026-07-25) + blank-slate ретест A0.6 холодным заходом (retest-01, 2026-07-28,
артефакт `sections/go/retest-a06/`).

## Журнал задач — продукт (outpost)

| # | Веха | Название | Статус | Дата |
|---|------|----------|--------|------|
| 11 | v0.1 | Вынос продукта в свой репозиторий `github.com/kahooso/outpost` (чистый старт, CI с gofmt, README) | ✅ выполнено | 2026-07-28 |
| 12 | v0.1 | Аудит `ports`: отмена контекста в `PortProbe.Run` (`DialContext`, проверка после сборщика), инъекция `Dialer` с фолбэком, тест на отмену, раскладка `main.go` по файлам, `BACKLOG.md` | ✅ выполнено | 2026-07-30 |

> С 2026-07-28 код продукта живёт в отдельном публичном репозитории `Desktop/outpost`.
> Здесь остаётся учебная часть: vault, скиллы, стратегия, артефакты задач.

## Algo-практика

Мини-задачи для закрепления базы. Не привязаны к блоку, идут параллельно.

| # | Тема | Паттерн | Дата |
|---|------|---------|------|
| algo-01 | Two Sum | map как индекс, O(N) | 2026-05-17 |

## Мок-собесы

| # | Дата | Темы | Файл |
|---|------|------|------|
| 01 | 2026-05-17 | A0.1–A0.5 (структуры, слайсы, указатели, ошибки, горутины) | [interview-01.md](../interviews/interview-01.md) |
| 02 | 2026-07-25 | A0.6–A0.9 (каналы+context, интерфейсы, stdlib CLI, net+worker pool) — часть 1 закрытия блока A0, заход «холодным» после 15 дней паузы | [interview-02.md](../interviews/interview-02.md) |
| retest-01 | 2026-07-28 | **Blank-slate ретест A0.6** (каналы + context с нуля) — часть 2 и последний гейт закрытия A0. Холодный заход, `ProcessAll` сам с первого раза; тесты — провал первой версии, зачёт со второй | [retest-01.md](../interviews/retest-01.md) |

## CTF

Параллельный трек picoCTF, фокус Web + General Skills (под AppSec). Темп — пользователь сам.
Одна строка на решённый таск. Скилл — `.claude/skills/ctf/skill.md`.

| # | Дата | Платформа | Категория | Таск | Главное |
|---|------|-----------|-----------|------|---------|
| *(пусто — трек заведён 2026-05-30, первый челлендж впереди)* | | | | | |

## Инфра-сессии

Сессии без учебной задачи — рефакторинг инструкций, скиллов, vault.
Не увеличивают счётчик задач с последнего мок-собеса.

| Дата | Что сделано |
|------|-------------|
| 2026-05-16 | Перестройка проекта: gitignore + CI, сокращение стратегии до A0+scout v0, переписаны skills (code-review/vault-write/session-debrief/give-task), консолидация memory, создан каркас `cmd/goscout/`, новый формат topics (goroutines/testing/runtime), HOWTO.md, escape hatch в `feedback_llm_dependency`. |
| 2026-05-17 | Мок-собес interview-01 (выявлены пробелы по errors.Is для не-обёрнутых, замыкания, pe vs &pe). Algo-практика добавлена как параллельная ветка: тестовая algo-01 (Two Sum) в `cmd/tasks/twosum/`, topic `topics/algo/map-as-index.md`. |
| 2026-05-30 | Ревизия goscout (тесты зелёные, vet чист — здоров). Перенято полезное из стороннего учебного репозитория, адаптировано под Go/AppSec: English-глоссарий в topics, CTF-трек (Web+General, скилл `ctf`), blank-slate ретест как гейт закрытия блока A0 (в `mock-interview`). |
| 2026-05-30 | **Вектор зафиксирован по учебному плану универа** (профиль «Технологии ИИ в кибербезопасности»). Цель уточнена: Security Go dev + AI/ML-безопасность. Модель «универ=дорога, Claude=зал». goscout переопределён как якорный продукт с целью (сетевой сканер + AI-модуль, версии v0.1→v1→v2 по семестрам). Создан `university-plan.md` (синхронизация универ↔goscout). Algo-ветка подтверждена (синхрон со «Структурами данных», сем 2). Память: `project_vector_ai_security`. |
| 2026-06-19 | Чистка `cmd/goscout/` перед фазой 2 A0.9: убраны учебные `// A:`/конспект-комментарии из `main.go`, мёртвый закомментированный код из `main_test.go`, обновлён `cmd/goscout/README.md` (статусы подкоманд, исправлена устаревшая сигнатура `Probe`). В vault `stdlib-cli.md` закрыт пробел про `bufio.Scanner` и `\r\n` (Windows). В скилл `code-review` добавлено правило: перед удалением учебного коммента из кода — Grep vault, иначе знание тихо теряется (пойман пользователем). |
| 2026-06-19 | **Вектор пересобран (широкая дорога).** Узкая цель «Security Go + AI/ML к 2029» → product/infra/deep-tech backend на Go; кибербез — сильный сквозной угол и точка входа (стажировка, Positive), не единственная цель. goscout: AI-модуль убран из roadmap, привязка версий к семестрам снята, форма = ближняя цель (сетевой recon CLI с security-уклоном) + открытый рост. Источник истины по карьере вынесен за пределы учебного репозитория. Правки: `CLAUDE.md`, `learning-strategy.md`, `university-plan.md`, корневой + `cmd/goscout` README, скилл `ctf`, `HOWTO.md`. Память `project_vector_ai_security` → переименована в `project_vector_backend_go` (+ обновлены входящие ссылки). Код не тронут, vet/тесты зелёные. |
| 2026-06-20 | **Инфра-сессия: уточнение формулировки вектора.** (1) Прописан протокол: карьерный вектор держится вне учебного репозитория, сюда приходит готовым и здесь не переписывается — блок триггеров в `CLAUDE.md`. (2) Правка «универ = **дорога** / следовать за универом» → «универ = **контекст тем**, не драйвер; темы по вектору, синхронизируя с универом». Места: `CLAUDE.md` §«Главная модель», `learning-strategy.md`, `university-plan.md` (таблица → «точки естественного совпадения», не дедлайн), память `project_vector_backend_go`, корневой README, скилл `give-task`. (3) Убрана сленговая метафора «Claude = зал» → буквальное «работа с Claude = практика на Go руками» во всех живых местах (CLAUDE.md, learning-strategy, university-plan, README, обе памяти) — чтобы формулировка читалась однозначно в любой сессии. Историческая запись 2026-05-30 не тронута. Код не тронут. |
| 2026-08-17 | **Стажировка PT Start + ревизия учебной механики.** (1) Разобраны условия трека «с обучением» (дедлайн заявки 20.08, 6 недель обучения → 2 недели знакомство с командами → тестовое → офер на 6 месяцев), направления оценены под вектор (ближе всего Threat Intelligence и Киберпогода), вопросы анкеты разложены по 4 блокам — всё в локальном рабочем документе по отбору (в репозиторий не публикуется). (2) Отобраны 8 механик обучения к внедрению — очередь в новом `.claude/upgrade-plan.md` (разделение статуса/плана, `plan-block`, классификация ошибок в ревью, `theory` как push, CODESTYLE для Go, гейтинг темпа + дисклеймер по темам, leetcode-трек, reading-code). (3) Решение: репозиторий готовится к публикации как есть, минус личное. Код не тронут. |
| 2026-08-18 | **Подготовка к публикации.** Вычищены упоминания приватного пространства вектора и третьих лиц из `CLAUDE.md` (раздел оркестрации заменён на «Вектор-вопросы — здесь не решаются»), `learning-strategy.md`, `university-plan.md`, `tasks/task-11.md`, скилла `ctf`, этого журнала. Переписан корневой README. Личные разборы провалов и мок-собесы оставлены сознательно — в них ценность репозитория. |
| 2026-08-20 | **Полная перестройка репозитория.** (1) Структура: `cmd/` → `sections/<домен>/<имя>/`, разбор задания переехал к своему коду (`task-a04-pointers.md` рядом с `pointers.go`); найдены три висячих пути, жившие с прошлого переезда. (2) Диспетчер: `learning-strategy.md` (437 строк, читался целиком каждую сессию) разложен на `roadmap/00-roadmap.md` (76, позиция), `roadmap/journal.md` (что сделано), `roadmap/archive.md` (закрытый блок A0), `strategy/learning-strategy.md` (только будущее); стратегия переехала в vault, `upgrade-plan.md` — в `.claude/`. (3) Внедрены все 8 механик из `upgrade-plan.md`: скиллы `plan-block` (блок = 11 заданий под наш темп, не 21), `theory`, `leetcode`, `reading-code`; `CODESTYLE.md` для Go; классификация природы ошибки и правило итеративных сессий в `code-review`; гейтинг темпа и дисклеймер «выполнено ≠ знаю» в CLAUDE.md; algo-трек с прогрессом по `[x]`. Скиллов 9 → 13. (4) Найдены и сняты противоречия: `code-review` и `give-task` предписывали аналогии с C/C++ вопреки CLAUDE.md и memory. (5) `LICENSE` (CC BY 4.0), README дописан пользователем. Проверки: `gofmt` чист, `go vet`/`go test -race` зелёные, 47 md-ссылок и 31 wikilink — битых нет. |

## Отчёты о сессиях

> Развёрнутые разборы: что сделано сам, что с подсказки, где провалы. Пишет `session-debrief`.
> Новые записи — сверху. Разбор конкретного задания — в его `task-*.md` рядом с кодом.

> **СЕССИЯ 2026-07-29/30 — task-12, ports доведён до отменяемого. Разбор — [../tasks/task-12.md](../tasks/task-12.md).**
> Первая работа после закрытия A0, целиком в `Desktop/outpost`. Режим по просьбе пользователя:
> продуктовый код пишет он, Claude — теория, контрвопросы, ревью, доказательства прогоном
> и исходниками stdlib (не рассуждением). Аналогии C/C++ не использовались.
> **Сам, без подсказок:** выбор точки проверки отмены после разбора вариантов; инъекция
> `Dialer` целиком (интерфейс с точной сигнатурой, `&net.Dialer` через указатель — не попался
> на pointer receiver, фолбэк с `p.Timeout`); раскладка по файлам и тестам; `.gitattributes`.
> **С разбора:** зачем `DialContext` вообще («забыл, что это»), где живут два бюджета времени,
> что означает `err == nil` в сканере — потребовалось объяснение функции целиком с нуля.
> **Провалы:** проверку `ctx.Err()` сначала поставил ДО цикла сборщика (дефект остался);
> тест на отмену трижды не проверял отмену; мутацию сам не прогнал; предложил слать в `results`
> только открытые порты — это deadlock, который он уже ловил в task-10 (в vault записан).
> **Итог:** тест дописан Claude по прямой просьбе пользователя («устал, ничего не понятно»).
> В коммит ушло 3 коммита (`chore` gitattributes, `docs` backlog, `feat` отмена+раскладка),
> всего не запушено 5. Пуш и тег — за пользователем.
>
> **СЕССИЯ 2026-07-28, часть 2 — ПРОДУКТ ВЫНЕСЕН В outpost.** Разбор — [../tasks/task-11.md](../tasks/task-11.md).
> Решение назвать продукт `outpost` и отделить его от учебного репозитория принято отдельно.
> `cmd/goscout/` → `github.com/kahooso/outpost` (`Desktop/outpost`), чистый старт без истории,
> три коммита, версия 0.1.0. Из goscout код удалён (история сохранена в коммите 774e4ae).
> **Рабочий процесс теперь:** сессия стартует в goscout (тут `.claude` и vault), outpost
> подключается `/add-dir C:\Users\ryche\Desktop\outpost`. Код → outpost (публичный),
> записи об учёбе → vault goscout (приватный). Vault один на все проекты.
> **Не запушено:** 5 коммитов в goscout + 3 в outpost — пуш делает пользователь.
> **Открытые вопросы к продукту:** лицензия (сейчас нет — формально «все права защищены»),
> тег v0.1 после пуша.
>
> **СЕССИЯ 2026-07-28, часть 1 — БЛОК A0 ЗАКРЫТ. Ретест A0.6 пройден ХОЛОДНЫМ заходом.**
> Разбор — [../interviews/retest-01.md](../interviews/retest-01.md), артефакт — `sections/go/retest-a06/`.
> Смягчение из interview-02 (перечитать `topics/` перед заходом) было разрешено, но пользователь
> им **не воспользовался** — `topics/` не открывал. Гейт взят в полной форме, сильнее плана.
> **Сам, без подсказок:** `ProcessAll` с первого раза — `WithTimeout`+`defer cancel()`, горутина
> на элемент со значением через параметр, **буфер на `len(items)`** (ключевое решение — принял
> сразу), сборщик на `len(items)` с `select`, частичный возврат. Единственный баг (`work(item)`
> вместо `work(s)`) нашёл сам после отказа отвечать на прямой вопрос.
> **Провал:** тесты. Первая версия таймаут не исполняла вообще, две тавтологии; по ходу разбора
> третья (де Морган, `&&` вместо `||`). Финальная версия — три свойства, **проверено мутациями**
> (два сломанных мутанта в песочнице, каждый пойман). Зачёт со второго захода.
> **Контрольный вопрос (небуф. канал + утечка) — частично:** проблему и последствие назвал сам
> (прогресс против interview-02), механику (состояние `chan send`, почему GC не спасает, почему
> рантайм молчит) — нет.
> **Со слов пользователя: «тесты самые сложные»** → вынесено сквозной темой выше.
> **Дальше:** закрытие блока A0 — веха уровня вектора, выбор следующего направления здесь не решаем.

> **СЕССИЯ 2026-07-25 (часть 1 закрытия A0) — мок-собес пройден, ретест ПЕРЕНЕСЁН.**
> Заход после 15 дней паузы, «холодным» (по выбору пользователя, без освежения vault) — сразу
> устный мок-собес A0.6–A0.9, 6 вопросов. Разбор — [../interviews/interview-02.md](../interviews/interview-02.md).
> **Держится сам:** интерфейсы A0.7 (зачем + неявная реализация + где проверяет компилятор —
> тема, которая раньше давалась трудно, закрыта), лексикографика vs числовая сортировка,
> panic на send в закрытый канал.
> **Просело за паузу (реактивировали в сессии):** A0.6 каналы — стартовая модель была неверной
> («небуф пишет в буфер и продолжает»), рандеву/`close`/goroutine leak собрались только по ходу
> разбора; причина «почему рантайм не кричит deadlock» была названа неверно.
> **Ретест A0.6 отложен на вечер 2026-07-25 / следующий заход** — пользователь устал (2 ч сессии)
> и сам выбрал сначала перечитать `topics/go/{goroutines,channels,context}.md`. Разрешено читать
> topics; НЕЛЬЗЯ — прошлый код `cmd/goscout/main.go` (dns/`runProbe`) и `tasks/task-06/07.md`.
> При фиксации ретеста честно пометить, что память была освежена (мягче «холодного» гейта).
>
> **Новое правило сессии:** обращение к пользователю — `kahooso` в КАЖДОМ ответе (канарейка на
> применение CLAUDE.md; memory `feedback_address_kahooso`).

> **task-10 ЗАКРЫТ (сессия 2026-07-10).** A0.9 пройдена целиком: `goscout ports` работает —
> worker pool (стадия 2a), числовая сортировка портов (2b), CLI `parsePorts` + флаги `--ports`/
> `--timeout` (стадия 3), весь вывод унифицирован на английском (stdout=данные/stderr=ошибки,
> префикс команды, `%w`-обёртка вместо имени функции, no-slog для CLI). build/vet/`-race` зелёные.
> Разбор — `tasks/task-10.md`, `topics/go/net.md`, `strings-sort.md`, `stdlib-cli.md`.
> Затыки сессии: лексикографика vs числовая сортировка, связка `main` (флаг → `parsePorts` →
> структура), порядок флагов `flag` (снова, было в task-09). Со слов: «пока не привык к работе» —
> недостаток налёта, не провал по теме.
>
> **ЗАКРЫТИЕ БЛОКА A0:** (1) мок-собес по A0.6–A0.9 — ✅ **сделан 2026-07-25** (interview-02);
> (2) **blank-slate ретест A0.6** — каналы+context с нуля, без подсказок (гейт закрытия,
> см. «Финал блока A0») — ⏳ **остался**. Если A0.6 не пишется без подсказок — точечный возврат,
> ретест повторить.
> После закрытия A0 → PART B обсуждения (аудит goscout / algo-02 Contains Duplicate / стажировки —
> вектор-уровень, здесь не решаем).
